# Retired animation source

`run-trip-recover-atlas.png` is intentionally excluded from the public build
and has no runtime references. It is retained here only as a future authoring
reference, per the project owner's request.

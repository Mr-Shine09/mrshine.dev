# Retired Hero V2 handoff location

This directory is historical documentation only. Its duplicate source frames,
previews, and runtime images were removed during the animation asset cleanup.

For all implementation work, use the single canonical map:

`site/public/assets/animation/ANIMATION_ASSETS.md`

The approved Hero V2 runtime files now live in
`site/public/assets/animation/hero/`, and its effects live in
`site/public/assets/animation/effects/`. Do not recreate the old asset tree.
